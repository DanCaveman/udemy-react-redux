import React from 'react';

import Modal from '../modal';

const StreamDelete = () => {
  return (
    <div>
      <Modal header="Delete Stream" content="Are you sure you want to delete this stream?" />
    </div>

  );  
};

export default StreamDelete;